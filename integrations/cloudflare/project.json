{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "@nodevault/platform.integrations.cloudflare",
  "sourceRoot": "integrations/cloudflare",
  "projectType": "library",
  "targets": {
    "lint": {
      "executor": "@nx/eslint:lint",
      "outputs": [
        "{options.outputFile}"
      ],
      "options": {
        "lintFilePatterns": [
          "integrations/cloudflare/**/*.ts"
        ]
      }
    }
  },
  "tags": []
}
